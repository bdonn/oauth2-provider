package oauth2.resource

// resource management

// fetch resource information from integrated system(legacy)
// and store the information to resource registry(cache)

// resource = service domain + bounded context(path) + resource identifier => URI
// scope = service domain + bounded context(path)
// target(requested by client) = URI??